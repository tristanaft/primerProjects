#include<iostream>
#include<vector>

//bool compare(matrix A, matrix B)
//{
//    return A==B;
//}

std::vector<int>::const_iterator 
change_val(int idx, std::vector<int> vec)
{
    
    for(auto p = vec.begin();idx!=0,p!=vec.end();idx--)
        p++;
    //so this will spit out a pointer to the vector element
    //of the given index??

 //i think i missed the point of this exercise? its actually really dumb.


}
